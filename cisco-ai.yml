---
- hosts: ansible-run
  tasks:
    - name: Execute command
      ansible.builtin.command:
        cmd: python3 /root/ansible/ai.py "{{ command }}"
      register: result


    - name: Display output
      ansible.builtin.debug:
        var: result.stdout_lines






