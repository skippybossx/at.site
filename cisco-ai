---
- hosts: docker
  tasks:
    - name: Execute command
      ansible.builtin.command:
        cmd: python /root/ansible/ai.py {{ command }}
      register: result


    - name: Display output
      ansible.builtin.debug:
        var: result.stdout_lines






